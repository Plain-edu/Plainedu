{
  "name": "plainedu-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd front && npm install && npm run build",
    "dev": "cd front && npm run dev",
    "preview": "cd front && npm run preview",
    "start": "echo 'This is a frontend-only project. Use build command instead.'"
  },
  "devDependencies": {},
  "dependencies": {}
}
