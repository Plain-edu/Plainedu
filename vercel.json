{
  "version": 2,
  "name": "plainedu-frontend",
  "buildCommand": "cd front && npm install && npm run build",
  "outputDirectory": "front/dist",
  "installCommand": "npm install",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
